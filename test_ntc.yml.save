---
  - name: PLAY - BACKUP
    hosts: ios
    gather_facts: no
    #connection: local

    tasks:

      - name: SAVE AND BACKUP CONFIGS
        ntc_save_config:
          local_file={{ filename }}
          platform={{ net_platform }}
          host={{ inventory_hostname }}
          username={{ un }}
          password={{ pwd }}
